<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.timemanga">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimeManga"
        tools:targetApi="31">

        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activity.NextActivity">
            <!-- Ajoute d'autres attributs d'activité si nécessaire -->
        </activity>

        <activity
            android:name=".Activity.ShonenActivity">
            <!-- Ajoute d'autres attributs d'activité si nécessaire -->
        </activity>

        <activity
            android:name=".Activity.SeinenActivity">
            <!-- Ajoute d'autres attributs d'activité si nécessaire -->
        </activity>

        <activity
            android:name=".Activity.ShojoActivity">
            <!-- Ajoute d'autres attributs d'activité si nécessaire -->
        </activity>

        <activity
            android:name=".Activity.JoseiActivity">
            <!-- Ajoute d'autres attributs d'activité si nécessaire -->
        </activity>

    </application>

</manifest>
